{% extends 'admin/base_popup.html.twig' %}
{% block title %}Modifier le dossier {{ image.titre }}{% endblock %}
{% block body %}
            <div class="container-fluid mt-3">
                <div class="col-lg-12">
                        <div class="card">
                            <div class="card-body">
                                <h4 class="card-title">Modifier cette ressource « {{ image.titre }} »</h4>
                                {{ include('admin/image/_form.html.twig') }}
                            </div>
                        </div>
                </div>
            </div>
{% endblock %}

{% block javascript %}
    <script>
        $(document).ready(function() {
            $("input").removeAttr("required");
        });
        $('#target_close').click(function() {
            window.close();
            opener.location.reload(true);
            
        });
    </script>
{% endblock %}