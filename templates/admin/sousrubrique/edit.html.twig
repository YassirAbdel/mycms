{% extends 'admin/base.html.twig' %}

{% block title %}{{ dossier.titre }} > {{ rubrique.titre }} > {{ sousrubrique.titre }}{% endblock %}

{% block body %}
           <div class="container-fluid mt-3">
                <div class="col-lg-12">
                        <div class="card">
                            <div class="card-body">
                                <h3>{{ dossier.titre }}</h3>
                                <h4 class="card-title">{{ rubrique.titre }} > {{ sousrubrique.titre }}</h4>
                                {{ include('admin/sousrubrique/_form.html.twig', {'button_label': 'Valider'}) }}
                                <!-- {{ include('admin/sousrubrique/_delete_form.html.twig') }} -->
                            </div>
                        </div>
                </div>
            </div>
{% endblock %}
