{% extends 'admin/base.html.twig' %}

{% block title %}Sousrubrique index{% endblock %}

{% block body %}
    <h1>Sousrubrique index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Resume</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sousrubrique in sousrubriques %}
            <tr>
                <td>{{ sousrubrique.id }}</td>
                <td>{{ sousrubrique.titre }}</td>
                <td>{{ sousrubrique.resume }}</td>
                <td>
                    <a href="{{ path('app_sousrubrique_show', {'id': sousrubrique.id}) }}">show</a>
                    <a href="{{ path('app_sousrubrique_edit', {'id': sousrubrique.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_sousrubrique_new') }}">Create new</a>
{% endblock %}
