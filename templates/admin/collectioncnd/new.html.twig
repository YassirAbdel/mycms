{% extends 'admin/base.html.twig' %}

{% block title %}Nouvelle collection{% endblock %}

{% block body %}
        <div class="container-fluid mt-3">
                <div class="col-lg-12">
                        <div class="card">
                            <div class="card-body">
                                <h3 class="card-title">Nouvelle collection</h3>
                                {{ include('admin/collectioncnd/_form.html.twig', {'button_label': 'Valider'}) }}
                            </div>
                        </div>
                </div>
            </div>

    <!-- <a href="{{ path('app_collectioncnd_index') }}">back to list</a> -->
{% endblock %}
